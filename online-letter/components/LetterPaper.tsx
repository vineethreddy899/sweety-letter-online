import PaperDoodles from "./PaperDoodles";
export type LetterPaperProps = { mode: "edit" | "read"; toName?: string; fromName?: string; body?: string; onChange?: (v: { toName: string; fromName: string; body: string }) => void };
export default function LetterPaper(p: LetterPaperProps) { const edit = p.mode === "edit"; return <div className={`lined-paper relative w-full max-w-3xl overflow-hidden rounded-sm shadow-paper-lifted ${edit ? "-rotate-[.3deg]" : "rotate-[.2deg]"}`}><div className="h-1 bg-gradient-to-r from-gold via-wax-light to-gold" /><PaperDoodles /><div className="relative p-8 pl-20 md:p-12 md:pl-24">{edit ? <><div className="mb-8 grid gap-5 sm:grid-cols-2"><label className="font-sans text-xs tracking-[.3em] text-wax-light">TO<input className="mt-2 w-full border-b border-wax-light/30 bg-transparent py-2 font-display text-xl outline-none" placeholder="Someone special" value={p.toName || ""} onChange={e => p.onChange?.({ toName: e.target.value, fromName: p.fromName || "", body: p.body || "" })} /></label><label className="font-sans text-xs tracking-[.3em] text-wax-light">FROM<input className="mt-2 w-full border-b border-wax-light/30 bg-transparent py-2 font-display text-xl outline-none" placeholder="With love" value={p.fromName || ""} onChange={e => p.onChange?.({ toName: p.toName || "", fromName: e.target.value, body: p.body || "" })} /></label></div><div className="mb-2 font-sans text-xs tracking-[.3em] text-wax-light">YOUR LETTER</div><textarea rows={14} maxLength={10000} placeholder="Dear..." className="w-full resize-none bg-transparent font-serif text-lg leading-[2rem] text-ink-soft outline-none" value={p.body || ""} onChange={e => p.onChange?.({ toName: p.toName || "", fromName: p.fromName || "", body: e.target.value })} /><div className="mt-2 text-right text-xs text-wax-light">{(p.body || "").length} / 10,000</div></> : <><h2 className="mb-6 font-display text-4xl text-ink">{p.toName ? `Dearest ${p.toName}` : "Dearest"}</h2><div className="flourish-divider mb-8" /><div className="whitespace-pre-wrap font-serif text-lg leading-[2rem] text-ink-soft">{p.body}</div></>}</div></div> }
