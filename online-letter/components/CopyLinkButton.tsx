"use client"; import {useState} from "react"; export default function CopyLinkButton({url}:{url:string}){const [copied,setCopied]=useState(false);const copy=async()=>{try{await navigator.clipboard.writeText(url);setCopied(true);setTimeout(()=>setCopied(false),1800)}catch{alert("Could not copy automatically. Please select and copy the link above.")}};const share=async()=>{if(navigator.share)await navigator.share({title:"You have a letter",text:"Someone sent you a letter. Open it when you're ready.",url})};return <div className="w-full max-w-xl"><div className="flex overflow-hidden rounded-2xl border border-wax-light/20 bg-white/70"><input readOnly value={url} className="min-w-0 flex-1 bg-transparent px-4 py-3 text-sm outline-none"/><button onClick={copy} className="bg-wax px-5 text-sm text-white">{copied?"Copied!":"Copy link"}</button></div>{typeof navigator!=="undefined"&&"share" in navigator&&<button onClick={share} className="mt-3 w-full rounded-full border border-wax-light/30 px-5 py-2 text-sm text-wax">Share</button>}</div>}
