"use client"; import {useState} from "react"; export default function PasswordModal({onCancel,onSubmit,error}:{onCancel:()=>void;onSubmit:(p:string)=>void;error?:string}){const [p,setP]=useState("");const [loading,setLoading]=useState(false);return <div className="fixed inset-0 z-50 grid place-items-center bg-ink/30 p-5 backdrop-blur-md"><form onSubmit={async e=>{e.preventDefault();setLoading(true);await onSubmit(p);setLoading(false)}} className="w-full max-w-md rounded-3xl romantic-bg p-8 shadow-paper-lifted"><div className="mx-auto mb-5 grid h-14 w-14 place-items-center rounded-full bg-wax text-2xl text-white">⌕</div><h2 className="text-center font-display text-3xl">This letter is sealed</h2><p className="mt-2 text-center font-serif italic text-ink-soft">Only those who know the secret can open it</p><input autoFocus type="password" placeholder="Enter the password..." value={p} onChange={e=>setP(e.target.value)} className="mt-7 w-full rounded-xl border border-wax-light/30 bg-white/60 px-4 py-3 outline-none focus:border-wax"/>{error&&<p className="mt-2 text-sm text-wax">{error}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onCancel} className="px-4 py-2 text-sm text-ink-soft">Cancel</button><button disabled={loading} className="rounded-full bg-wax px-5 py-2 text-sm text-white">{loading?"Opening...":"Open letter"}</button></div></form></div>}
